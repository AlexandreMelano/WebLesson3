/* eslint "no-console: 0" eslint "spaced-comment: 0 */

const accounting = require('accounting');

/* create a variable and assign it a numeric value*/
const amount = 98.3456;

/* display the amount */
console.log(accounting.formatMoney(amount));